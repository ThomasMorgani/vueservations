//On initial load...
  -get inventory
  -get reservations
    -how many? last, current, next month? all?
  -create cat:details mapping array
    -this is to iterate over item details in frontend so it knows what values to display

-seperate items into categories
  -enable add/remove/edit categories

-event detail popup
  -add patron: first, last then expandable details
  -add item: ABV then expandable details

-map cat: key? set item cat key in item list


in settings add uup/down arrow to manually set height of calendar
  -save to local storage